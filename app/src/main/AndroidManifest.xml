<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.pmino">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.PMINO">
        <activity android:name=".code"></activity>
        <activity android:name=".Reini" />
        <activity android:name=".rech" />
        <activity android:name=".Confirmation" />
        <activity android:name=".Liste" />
        <activity android:name=".Choix" />
        <activity android:name=".Inscription" />
        <activity android:name=".Compter" />
        <activity android:name=".Identification" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>